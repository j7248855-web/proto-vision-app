syntax = "proto3";
package media;

option go_package = "./gen/proto;media_pt";

message VideoStream {
    int32 camera_id = 1;
    bytes data = 2;
    uint32 timestamp = 3;
    bool is_keyframe = 4;
}
message Status {
    bool status = 1;
}
service MediaStream {
    rpc Stream(stream VideoStream) returns (Status);
}